# 2026-09-24

## 状态确认（开工第一件事）

- 远端 `refs/heads/master` = `c908199` = 本地 HEAD ✅ —— **昨天卡住的推送已经解决了**（`bdaa517`/`ea59cb8`/`c908199` 全在远端）。
  昨天那次 `schannel: CRYPT_E_NO_REVOCATION_CHECK` 是间歇性的，不是仓库问题。以后遇到同样报错：先 `git ls-remote --heads github` 比一次再下结论。
- 工作区干净，`src/` 7210 行 / 51 文件，部署包 `dist/QShot/` 启动冒烟 `exit=124`（存活未崩）。
- `src/` 里 **TODO/FIXME/XXX/HACK 零命中**。

## 用户要的是「评估下一步开发计划」

不是让我开工，是要判断。给了结论 + 依据 + 明确不做。

### 核心结论：功能面已经打平，下一步不是加功能

剩余工作三类，**没有一类是功能**：

| 类别 | 数量 | 内容 |
| --- | --- | --- |
| **人工确认债** | `ROADMAP.md` 里 **12 处 ⏳** | 贴图手感、阴影深浅、图标造型、历史记录三个默认值、荧光笔 alpha、双屏行为、多屏焦点…… |
| **工程债** | 3 项 | 探针在 gitignore 目录里（627 条断言跑不起来）、无 CI、P1-2/N-9 的卫生项 |
| **分发债** | 2 项 | 安装包从未编译（本机无 Inno Setup）、无代码签名 |

**功能缺口**只剩延迟截图（抓右键菜单/悬停态需要它）—— 而且这个也不该现在做，等真用一天再说。

### 建议顺序与理由

1. **真用一天**（成本 0，信息量最大）。12 处 ⏳ 全是同一个根因：**这台工具从没被人当日常工具用过**。
   627 条断言全在验证机制，没有一条能回答「用起来顺不顺手」。用一天，那 12 处一次消化掉，
   并且会暴露真缺陷 —— 这是唯一能产生新信息的动作。
2. **把探针搬进仓库 + 接 CTest**。「无测试无 CI」的正解**不是**新写一套 QtTest（会和 627 条重复，
   正好又造出「同一个东西两处维护」），而是让已有的断言谁都能跑。注意：`.cpp` 进仓库，
   65 张证据 PNG 不进（二进制churn）。
3. **安装包真编译一次** —— 需要先装 Inno Setup。解锁「能发给别人」。
4. **签名** —— 要买证书，是他的决定，不是我的。

### 明确不建议

- 重开滚动长截图 / OCR —— 已决策推迟，且有硬理由（MinGW 编不了 WinRT OCR；滚动拼接固有难点）。
- 加更多标注工具 —— 八种已经过剩。
- 写独立 QtTest 套件 —— 与 627 条探针断言重复。
- 追「全能」的广度 —— 他自己的规则是 YAGNI。

### 顺带发现：`ROADMAP.md` §1 有个无出处的过期数字

「**已覆盖 2 / 6 个用户场景**」—— 这句话在 `git log -S` 里只能追到 `6d0ec05`（首次提交），
**那 6 个场景是什么，全仓没有任何地方定义过**。而且它与同一页的「已有能力」自相矛盾：
贴图（M3）、历史记录（M4）、序号/高亮（M5）都已完成，覆盖率不可能还是 2/6。

这和审查文档那批过期状态是**同一个病**：一个没有依据的结论被写进文档，之后没人能证伪。
建议要么补上那 6 个场景的定义并重算，要么删掉这个数字。

## 记忆维护

- 昨天（09-23）那条「推送待补推」的结论已作废，本条已更正。

---

## 下午：执行「建议 2」—— 探针入库 + 接 CTest

用户选了「先按建议 2 动手」。目标：让 627 条已有断言谁都能跑，**不新写测试套件**（那会和探针重复）。

### 做了什么

| 改动 | 内容 |
| --- | --- |
| `.gitignore` | 根因修复：`build/` `build-*/` 加**前导斜杠**。不加时 glob 在**任意层级**匹配，吞掉了三个源码目录 |
| `CMakeLists.txt` | app 源码收进静态库 `qshot_core`（清单只写一次）；`include(CTest)` + `qshot_add_probe()`；19 个测试 |
| `tools/smoke_deploy.sh` | 改走 `cmake --build --target probe_deploy` |
| `build-review/build_probes.sh` | **已删除**（两套构建同一批源 = 两处维护） |
| 探针修复 | `probe_magnifier_render` 3 处硬编码路径、`probe_quiet_save` + `render_history_menu` 缺 `fflush`、`probe_settings_autostart` 过期期望值 |

### 三个真实发现

1. **`probe_quiet_save` 静默通过（最重）。** 它跑完 29 条断言、`exit=0`，但 stdout **一个字节都没到管道**。
   旧脚本只对输出做正则求和，匹配不到就跳过，于是报 **598 而非 627** 且全绿。插桩证明（MARK-A/B/Z 走完、
   `checks=29 failures=0`，stdout 0 字节）；显式 `fflush(stdout)` 后 2164 字节全部出现。
   **结构性防护**：CTest 要求每个测试打印 `N checks, 0 failures`（`PASS_REGULAR_EXPRESSION`）——
   汇总行是返回前最后一行，匹配到它就等于「跑到末尾且零失败」，比退出码更强。
   **627 = 记忆里记的历史全量**，所以旧脚本报的 598 才是错的。

2. **`probe_settings_autostart` 从没跑过，一跑就失败。** `pen colour stored` 期望 `#1e88e5`，
   实际是 `#ff1e88e5` —— `Settings` **故意**用 `HexArgb`（源码注释：`name()` 会丢掉荧光笔的 alpha）。
   **是期望值过期，不是产品错。** 它也是唯一覆盖 `WinAutoStart` 的测试，之前零覆盖。

3. **`render_panels`（fixture 生成器）从没被登记**，而 `render_history_menu`/`render_pin` 都读它产的
   `panel_zh_text_toolbar.png`（有优雅降级，所以一直没暴露）。

### 踩到的坑（都实测过）

- ⚠️ **`PASS_REGULAR_EXPRESSION` 不能加 `^...$`**：CTest 对**整段**输出做一次匹配、不是逐行。
  带锚点时 19 个测试**全红**（输出里明明有 `93 checks, 0 failures`），去掉后全绿。
- ⚠️ **`set_tests_properties` 是 `属性 值` 成对的**：列表型属性（`ENVIRONMENT_MODIFICATION`）写成两个
  独立参数 → 最后一个值成了没属性名的孤儿 → 报无用的 `incorrect number of arguments`。先放进变量再整体传。
- ⚠️ **`probe_deploy` 要用 `QApplication`**，所以得显式链 `Qt6::Widgets`（它不链 `qshot_core`）。
- ⚠️ **`.gitignore` 的 glob 不加前导斜杠会在任意层级匹配** —— 除了 `build-review/`，还吞了
  `.agents/skills/build-verify/`（0 个跟踪文件，那个 skill 一直没进过仓库）和 `building-data-apps/`
  （文件已跟踪所以没丢，但**新文件会被静默忽略**）。

### 验证结果

- `ctest` → **19/19 通过**，**627 checks / 0 failures**，11 秒（旧脚本全量约 8 分钟）
- 逐目标断言数与基线**逐条一致**，`probe_quiet_save` 从「数不到」变成 **29/0**（598 → 627 的唯一差额）
- `--clean-first` 全量重编：**64 个编译单元 / 0 警告 / 0 错误**
- `archaeology` 目标：20 个历史探针**全部链接成功**（0 错误 0 警告）
- `bash tools/smoke_deploy.sh` 端到端通过（10 checks / 0 failures）

### 顺带修正的过期数字

- `ROADMAP.md` 第 224 行（M4 历史记录）标注了命令已被取代，保留当时结果
- `ROADMAP.md` 平台实现「**四个**」→ 实测是 **五个**（`PlatformFactory` 有 5 个 create* 方法）
- `.agents/skills/build-verify/SKILL.md` 重写：它原本建议 `cmake -B build-review`（会在源码目录里
  建构建树）、说「默认构建不带告警」（早已不成立）、教人手写 g++ 命令行（已被 CMake 取代）

