# 2026-09-21

## 仓库清理：移除 agent 开发遗留文件

用户要求清理「与项目无关、agent 开发所遗留的文件」，明确 **rules 和 skills 不删**。
处理前先做了完整盘点（git status / git check-ignore / du -sh），列清单让用户用选项确认，再动手。

### A 类：模板脚手架残留 —— 已 `git rm`（历史可恢复）

| 文件 | 判断依据 |
| --- | --- |
| 根 `main.cpp` | Qt Creator「Qt Quick Application」模板入口：`QQmlApplicationEngine` + `loadFromModule("QShot","Main")`。真实入口是 `src/main.cpp`，CMakeLists 只编译后者 |
| 根 `Main.qml` | 同模板 Hello World 窗口（`title: "Hello World"`、`InputPanel` 虚拟键盘）。项目是纯 Qt Widgets |
| `build_output.txt` | 早期构建日志，记录的是一次 `mocs_compilation.cpp` **失败**，留着会误导 |
| `err.txt` / `out.txt` | 均为 **0 字节** 空文件 |
| `importedcontent/README.md` | Figma→Qt 导入占位说明，自称「主 CMakeLists 会自动拾取」，但 CMakeLists **并未引用**该目录 → 死目录 |

同时删除了空掉的 `importedcontent/` 目录。

### B 类：构建/索引产物 —— 已删除（gitignore 内，可重新生成）

- `build-mingw/`（2.6M）：agent 早期建的**第二个**构建目录，含旧 `qshot.exe`。正式构建目录是 `build/`。
- `.cache/clangd/`（243K）：clangd 索引缓存（其 `index/.gitignore` 自带 `*`，所以 git 从不显示它）。
- `build-review/`：**清产物、留探针源码**。删掉 12 个 `.exe` + `build.ninja` + `.ninja_deps` + `.ninja_log` + `CMakeCache.txt` + `cmake_install.cmake` + `CMakeFiles/` + `qshot_autogen/` + `.qt/`，体积 **25M → 485K**。**保留** 24 个探针/渲染源码（`probe_*.cpp`、`render_*.cpp`、`dprtest.cpp`）+ 42 张 PNG 证据图。

### C 类：保留

`.agents/rules/`、`.agents/skills/`（用户指定不删）、`.workbuddy-ai/`、`docs/`、`src/`、`resources/`、`CMakeLists.txt`、`README.md`、`build/`、`.vscode/settings.json`、`.qtcreator/CMakeLists.txt.user`。

### 验证

- `grep -rn "Main\.qml\|importedcontent\|loadFromModule"` 全仓无引用，仅三份审查报告作为历史记录提及。
- 重新构建通过：`[100%] Built target qshot`。
- 仓库总体积约 102M → 75M（省 ~27.6M）。

### 附带改动：`.gitignore` 新增

```
# Local tooling caches
.cache/

# Qt Creator template leftovers (real entry point is src/main.cpp)
/main.cpp
/Main.qml
/importedcontent/
/*_output.txt
/err.txt
/out.txt
```

至此 **ROUND3 审查报告 P3（仓库残留）已闭环**。注意：三份报告里的 P3 描述是当时的快照，未回改。

---

## 事故与恢复：`git rm` 连带清空了整个 `docs/` 目录

### 事故经过

用户确认「直接删掉」过期的 `docs/ARCHITECTURE.md`、`ROADMAP.md`、`TASKS.md` 后，执行：

```bash
git rm -q docs/ARCHITECTURE.md docs/ROADMAP.md docs/TASKS.md
```

结果：**git 索引只正确暂存了这 3 个文件**（`git status` 显示 `D docs/ARCHITECTURE.md` 等三行），
但**工作区里整个 `docs/` 目录被清空**，连带删掉了未被要求删除的
`docs/CODE_REVIEW.md`、`docs/CODE_REVIEW_ROUND2.md`（已跟踪）和
`docs/CODE_REVIEW_ROUND3.md`、`docs/SETTINGS.md`（**未跟踪**）。

### 成因判定

翻 `~/.workbuddy-ai/audit-log/spool/*.jsonl`（`sessionId` = 本次会话）逐条核对：
**本次会话执行过的命令里没有任何一条显式删除 `docs/`**，唯一碰过 `docs/` 的就是那条 `git rm`。
「索引正确 / 工作区过度删除」的不对称，指向 `git rm` 调用本身（该命令当时带
`Sandbox bypassed (escalation-approved)` 标记），而非 git 的正常语义。**具体机制未能定位，按「已知风险」对待。**

### 恢复手段（关键，可复用）

| 来源 | 能救回什么 |
| --- | --- |
| `git checkout HEAD -- <path>` | 已跟踪但被工作区删除的文件（救回 CODE_REVIEW.md / CODE_REVIEW_ROUND2.md） |
| **`~/.workbuddy-ai/file-history/<sessionId>/<hash>@vN`** | **agent 写过的文件的历史快照，未跟踪文件也能救** ← 本次靠这个救回两份未跟踪文档 |

定位方法：先在 `~/.workbuddy-ai/changes-index/<sessionId>.json` 里搜文件名，确认该会话动过它；
再在 `~/.workbuddy-ai/file-history/<sessionId>/` 下按特征词 grep 快照内容，挑体积与原件一致的最新版本拷回。

本次命中的快照：`ddefc5e9fb3b9643@v4`（21814B，ROUND3）、`de5c50748149bb72@v3`（8652B，SETTINGS）。
**恢复校验通过**：字节数与原件一致，行数与 `changes-index` 记录的 `additions` 一致（325 / 98）。

失败的死路（不必再试）：`git fsck --lost-found` 的悬空 blob 里没有这两份；
VSCode `User/History` 只有 5 条且与本项目无关；回收站无；无 stash；临时目录无。

### 教训

1. **未跟踪文件是 git 无法兜底的单点。** 动 `docs/` 这类混装「已跟踪 + 未跟踪」的目录前，先把未跟踪文件复制到仓库外。
2. **`git rm` 之后必须立刻 `ls` 该目录**，不能只看 `git status`——本次 `git status` 一切正常，是 `ls -la docs/` 才暴露的。
3. 更稳的替代做法：用 `rm <file>` 删文件，再 `git add -A <dir>`，避开 `git rm` 的目录处理路径。
